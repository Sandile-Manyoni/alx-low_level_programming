#include <stdio.h>
/**
 * main - A progrmam that print using printf function
 * Return: 0 (once 0 is return, program is successful)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
